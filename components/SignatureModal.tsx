// This component appears to be a duplicate or an old version of SignatureConsentModal.
// Re-exporting SignatureConsentModal to resolve the dependency.
import SignatureConsentModal from './SignatureConsentModal';
export default SignatureConsentModal;